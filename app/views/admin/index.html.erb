<h1> hello <h1>


<%= @users.all.map(&:attributes) %>
<%= Product.all.map(&:attributes) %>

 <% @categories.each do |category| %>
 <%= link_to category.category_name, root_path + "?category_id=#{category.id}" %>
 <% end %>



